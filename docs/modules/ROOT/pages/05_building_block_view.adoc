= 5. Building Block View

== 5.1 Whitebox: Overall System

....
F:\Project (npm workspaces)
├── client/          React + TypeScript + Vite SPA
└── server/          Express 5 + better-sqlite3 + Claude API proxy
....

The layering below is the one rule the whole server obeys: routes validate and
map status codes, services own the domain, and only `db.ts` opens SQLite. The
MCP adapter (ADR-19) enters through the same REST front door as the browser —
it is a second client, not a second back end.

[plantuml,building-blocks,svg]
....
@startuml
skinparam componentStyle rectangle
skinparam shadowing false
skinparam wrapWidth 150
skinparam defaultTextAlignment center
skinparam packageStyle rectangle
hide stereotype

actor "User" as user
component "MCP client\nClaude Code / Desktop" as mcpClient

package "client/ — React SPA" {
  component "pages + components\nDraw, Tasks, Goals,\nStats, Settings" as pages
  component "lib/ — pure logic\ndrawable, focusView,\ndrawnCard, localDay, rarity" as clib
  component "hooks/ — TanStack Query\nuseTasks, useDraw, useTimer,\nuseGamification, useAi" as hooks
  component "api/client.ts\ntyped fetch wrapper" as apiClient
}

package "server/ — Express 5" {
  component "tools/ — MCP adapter\nno domain logic (ADR-19)" as mcpAdapter
  component "routes/ — HTTP layer\ntasks, draw, timer, goals, stats,\nactivity, gamification, shop, ai, backup" as routes
  package "services/ — domain logic" {
    component "drawService\ndrawability, weights,\nwarm-up, current draw" as drawSvc
    component "gamificationService\ncompleteTask(): XP,\nstreaks, achievements" as gamSvc
    component "shopService + packCatalog\natomic opening/replay + derived snapshot\npure catalog/probability rules" as shopSvc
    component "statsService\nactivityService" as statsSvc
    component "aiService\ncardArtService" as aiSvc
    component "backupService" as backupSvc
  }
  component "db.ts\nSQLite bootstrap, migrations" as dbmod
}

database "app.db\nSQLite (WAL)" as sqlite
folder "data/files/" as files
cloud "Claude API" as claude

user --> pages
pages --> hooks
pages --> clib
hooks --> apiClient
apiClient --> routes : "JSON REST /api/*"
mcpClient --> mcpAdapter : stdio
mcpAdapter --> routes : "HTTP 127.0.0.1:3001"
routes --> drawSvc
routes --> gamSvc
routes --> shopSvc
routes --> statsSvc
routes --> aiSvc
routes --> backupSvc
drawSvc -[hidden]-> gamSvc
gamSvc -[hidden]-> shopSvc
shopSvc -[hidden]-> statsSvc
statsSvc -[hidden]-> aiSvc
aiSvc -[hidden]-> backupSvc
drawSvc --> dbmod
gamSvc --> dbmod
statsSvc --> dbmod
aiSvc --> dbmod
backupSvc --> dbmod
dbmod --> sqlite
routes --> files
aiSvc --> claude
pages -[hidden]-> clib
clib -[hidden]-> hooks
@enduml
....

== 5.2 Whitebox: Server

....
server/src/
├── index.ts                 dev entry: loads .env, starts the API server (loopback, always)
├── prod.ts                  production entry (#189): same server + built client, one port;
│                            the only entry that honors HOST
├── config.ts                HOST/API_PORT env resolution (default 127.0.0.1; PORT ignored)
├── server.ts                startServer() — binds the given host (default loopback):API_PORT
├── app.ts                   createApp() — Express app + router mounting; serves
│                            client/dist with SPA fallback in production (ADR-49)
├── db.ts                    SQLite bootstrap, PRAGMA user_version migrations
├── schema.sql               Current full schema (fresh installs)
├── aiSchemas.ts             zod schemas for structured AI outputs
├── routes/                  HTTP layer (validation, status codes)
│   ├── tasks.ts             CRUD, subtask bulk-create, timer start, completion entry point,
│   │                        card-art endpoints (cache-or-generate + regenerate, #113)
│   ├── categories.ts        CRUD (delete blocked while referenced)
│   ├── goals.ts             CRUD with task/material counts
│   ├── materials.ts         multer upload, download, delete (disk + row)
│   ├── timer.ts             current/stop; start lives on tasks route
│   ├── draw.ts              POST /api/draw, POST/GET /api/draw/warmup (#57),
│   │                        GET /api/draw/current (restore)
│   ├── stats.ts             GET /api/stats (UTC date-range handling),
│   │                        GET /api/stats/estimation-bias (all-history, ADR-27)
│   ├── activity.ts          GET /api/activity (per-local-day history, ADR-21)
│   ├── gamification.ts      GET /api/gamification
│   ├── shop.ts              exact Gold/Ticket pack HTTP contract + collection snapshot;
│   │                        validates before the atomic shop service (#266)
│   ├── settings.ts          key-value settings
│   ├── backup.ts            GET /api/backup/export, POST /api/backup/import (ADR-26)
│   ├── cardArt.ts           GET /api/card-art — cache-only batch reads (#114),
│   │                        deliberately not under /api/tasks/:id (never generates)
│   └── ai.ts                status, key, estimate, breakdown, plan-goal, generate-tasks
└── services/                Domain logic
    ├── drawService.ts       drawable predicate + weight formula + roulette selection + current draw
    ├── localDay.ts          the server's local calendar day (streaks): pure
    ├── statsService.ts      SQL aggregations (minutes by impact/category/goal), estimation
    │                        accuracy + per-category bias (coaching input, ADR-27)
    ├── activityService.ts   per-day card derivation for the Stats History calendar (pure core + SQL fetch)
    ├── gamificationService.ts  completion transaction, XP/levels/streaks, achievements
    ├── streak.ts            pure streak fold: rest days, freeze tokens (ADR-28)
    ├── shopService.ts       per-request immediate pack transaction, replay, derived snapshot,
    │                        settings-owned collection/equipment and durable effects (#266)
    ├── packCatalog.ts       pure fixed catalog, Secret progression, rarity/item selection,
    │                        duplicate refund and effective bonus selection (#265, ADR-62)
    ├── backupService.ts     zip export/import of app.db + files/ (ADR-26)
    ├── aiService.ts         Anthropic client, material blocks, prompts, token guard
    ├── aiPostprocess.ts     deterministic generate-tasks shaping (ADR-14): pure, SDK-/DB-free
    ├── svgSanitizer.ts      allowlist re-serializer for model-written SVG (ADR-22): pure
    ├── cardArtStyle.ts      deterministic per-task style directives (#113): pure
    └── cardArtService.ts    card-art cache orchestration (generate → sanitize → store once;
                             #113 regenerate replaces, generations coalesce per task)
....

[cols="1,2,2",options="header"]
|===
|Block |Responsibility |Key invariant

|`drawService`
|Candidate selection + weighting
|Drawability is computed, never stored

|`gamificationService`
|Completion side-effects
|XP/level/streak always derived from `completions`; freeze tokens are an append-only earn log, consumption derived at read time (ADR-28)

|`statsService`
|Time aggregation, estimated-vs-tracked accuracy
|Running entries count up to _now_; date math in UTC; tasks without estimate or tracked time are excluded from accuracy, never counted as 0

|`activityService`
|Per-day history card derivation (History calendar)
|Derived read-side from `time_entries` + `completions`, bucketed by *local* day; a worked-but-uncompleted day stays face-down forever

|`shopService` + `packCatalog`
|Atomic pack purchase/replay, derived shop state and settings-owned collection, over pure catalog/probability calculations
|Classic is always-owned fallback and never pullable; each fresh opening is one immediate transaction and three server-supplied samples, while replay consumes no RNG and reapplies no fact

|`aiService`
|Claude API access
|Requests blocked without key (503) or above 180K input tokens (400)

|`svgSanitizer` + `cardArtService` + `cardArtStyle`
|Card art safety, cost and variety (ADR-22)
|Only allowlist-re-serialized SVG is ever stored or served; at most one generation *in flight* per task; style directives are a pure function of the task id; regenerate is the only path that replaces a row (generate-then-replace)
|===

=== Gold pack catalog boundary (#265)

`packCatalog.ts` owns one registry in this exact order: `classic` (Classic
weave); common `ember` (Ember lattice), `tide` (Tide glass), `midnight`
(Midnight stars), `parchment` (Aged parchment), `graphite` (Graphite weave);
rare `meadow` (Meadow braid), `royal` (Royal filigree), `sakura` (Sakura
glass), `circuit` (Neon circuit), `frost` (Frost lattice); ultra-rare `aurum`
(Aurum crest), `aurora` (Aurora silk), `obsidian` (Obsidian gold); and
secret-rare `prism` (Prism foil). Classic is the common fallback only, leaving
pullable tier sizes 5/5/3/1.

Its functions are side-effect-free and require explicit finite `[0,1)`
samples: trailing non-Secret outcomes increase the next Secret chance from
500 by 50 basis points to a 1500 cap; rarity selection reserves that Secret
chance and divides the remainder 45:32:18; a separate sample selects uniformly
within the chosen tier; duplicate Gold is 10/20/40/100; and bonus intervals are
Freeze 5%, Pouch 5%, Ticket 2%, none 88%, with a would-be Freeze resolving to
Pouch at the bank cap of 2. Invalid inputs throw rather than clamp or coerce.
`packCatalog.ts` still owns no RNG or persistence. #266 composes `Math.random`
at `createApp` into a per-app shop router and calls the pure functions with
exactly three samples only after replay and payment checks. `shopService.ts`
owns the current-handle `db.transaction(fn).immediate()` boundary: canonical
ref lookup, derived Gold/Ticket/Secret/Freeze reads, opening insert,
settings-owned grant, and charge/refund/Pouch effects commit or roll back as
one unit. It maps immutable opening facts separately from the fresh shop
snapshot; same-ref/same-payment replay returns the same opening without RNG or
repair. No client, schema, backup-format, or test endpoint participates.

== 5.3 Whitebox: Client

....
client/src/
├── api/client.ts + types.ts   typed fetch wrapper, shared DTOs
├── hooks/                     TanStack Query wrappers (useTasks, useDraw,
│                              useTimer, useGamification, useGoals, useAi,
│                              useActivity, useEstimationBias, useShop) plus the
│                              tab-scoped pack purchase-intent/retry helper (#267)
├── pages/                     Draw (centerpiece, incl. the CSS-3D card flip),
│                              Tasks (capture-first: quick capture + triage strip +
│                              category tree, ADR-40), Goals, Stats (summary tiles,
│                              time bars, estimation summary, History calendar,
│                              achievements — ADR-41), Settings
├── lib/                       pure client logic (drawable classification, effort display,
│                              generate-tasks review model,
│                              shared local-day math, estimation coaching decisions,
│                              timer/countdown formatting + focus-view decision,
│                              trophy rarity tiers) — unit-tested
│                              without rendering
└── components/                TaskRow/Form/Badges, SubtaskEditor, EstimateInput
                               (inline triage estimate), TimerBar,
                               FocusOverlay (fullscreen focus mode, ADR-29),
                               EmptyPoolReason (one voice for an empty pool),
                               GamificationHeader, TrophyDeck, HistoryCalendar,
                               AchievementToast, ShopPanel (authoritative Gold/Ticket
                               controls and shared back swatches), PackRevealModal
                               (current-response portal, focus/inert and ordered cards)
                               with a pure identity-guarded reveal state helper,
                               MaterialsSection, AiSuggestionPanel
                               (breakdown, plan, generate-tasks panels)
....

All server state flows through TanStack Query. The independent `['shop']`
snapshot is replaced only by a successful pack response or refetched after an
authoritative producer/equip invalidation; the pack client never derives or
optimistically changes economy/collection state. A validated purchase response is
also snapshotted into one mount-local reveal session keyed by its opening ref/order;
that presentation never rereads mutable query data or persists reveal progress.
Task mutations invalidate the
`tasks`/`gamification`/`stats`/`activity`/`goals` query keys (goal cards derive
their task counts from tasks; timer start/stop also touches `activity` — a
started task lights up today's cell in the History calendar), plus `draw.current`,
which holds a pointer a task mutation can invalidate (ADR-13). There is no
other state management.

== 5.4 Data Model

....
categories 1─n tasks n─1 goals 1─n materials
                │ 1─n time_entries
                │ 1─n completions        achievements (unlocked keys)
                └─ self-reference: parent_id (breakdown tree, depth 1)
                                         settings (key/value)
....

Central semantics: completing a *recurring* task keeps it `open` and pushes
`due_date` forward — that date is the task's *next occurrence*, so the card is
out of the deck until it arrives (ADR-6 amendment, #205); completing a normal
task sets `status='done'`. Both insert a
`completions` row — the single source for XP, streaks, trophy deck, and history.
