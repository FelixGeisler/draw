= 6. Runtime View

== 6.1 Drawing a card

[plantuml,runtime-draw,svg]
....
@startuml
skinparam shadowing false
skinparam sequenceMessageAlign left
autonumber

actor User
participant "DrawPage\n(+ useDraw / useCurrentDraw)" as UI
participant "routes/draw.ts" as Route
participant "drawService" as Draw
participant "gamificationService" as Gam
database "SQLite" as DB

== Draw (the gamble) ==
User -> UI : click the deck
UI -> Route : POST /api/draw {categoryId?, goalId?}
activate Route
Route -> Draw : drawTask(filters)
activate Draw
Draw -> DB : queryCandidates(): open leaf, estimated,\n<= max_draw_effort, not blocked/snoozed,\nnot held back behind a sequential sibling
DB --> Draw : candidate rows
Draw -> Draw : window filter (#33, local wall clock)
alt pool empty
  Draw -> DB : clear current draw
  Draw --> Route : {task: null, reason: no_ready_tasks |\nall_too_big | all_outside_window}
else candidates present
  Draw -> Draw : poolWeights(): impact²/effort × urgency\n× staleness × cooldown × siblingDamping(√k)
  Draw -> Draw : roulette pick
  Draw -> DB : UPDATE tasks SET last_drawn_at
  Draw -> DB : setSetting(current_draw = id),\ndrop any warm-up marker (ADR-13)
  Draw --> Route : {task, poolSize, probability}
end
deactivate Draw
Route -> Gam : checkAchievements({drew: true})
Gam --> Route : newAchievements[]
Route --> UI : {task, poolSize, probability, newAchievements}
deactivate Route
UI -> UI : shuffle animation, then write-through\nqc.setQueryData(["draw","current"])
UI --> User : card flips face up — no re-draw (#88)

== Restore / dismiss (derived, #110) ==
UI -> Route : GET /api/draw/current\n(on mount, every 60s, on window focus)
activate Route
Route -> Draw : currentDraw()
activate Draw
Draw -> DB : read pointer + task row
Draw -> Draw : isRestorable()? — same predicate as the pool
alt still drawable
  Draw --> Route : {task, warmup?}
else stale (completed/snoozed/edited out of the deck elsewhere)
  Draw -> DB : clear pointer lazily
  Draw --> Route : null
end
deactivate Draw
Route --> UI : the standing card, or null
deactivate Route
UI --> User : reload restores the revealed card (no shuffle);\na pointer cleared elsewhere dismisses it
@enduml
....

Empty pool returns
`{task: null, reason: 'no_ready_tasks' | 'all_too_big' | 'all_outside_window'}` —
the UI turns `all_too_big` into a "break something down" call to action, while
`all_outside_window` (every remaining candidate is outside its availability
window, ADR-20) says "scheduled for later": those cards return on their own.

The pick is persisted as the *current draw* (ADR-13). The DrawPage DERIVES
the standing card from `GET /api/draw/current` continuously (#110, ADR-29
amendment): a reload restores it — revealed, no shuffle, the same pattern as
the TimerBar (6.4) — and a pointer cleared by a completion, snooze, or
delete on another surface (TimerBar, Tasks page, MCP, a second tab)
dismisses the card on the query's next refetch without a second ✓ Done.
The endpoint validates the task is still
drawable (open leaf, estimated, within `max_draw_effort`, not snoozed/blocked,
not held back behind a sequential sibling, inside its availability window) and
lazily clears a pointer that went stale, e.g. because the card was edited out
of the deck.

A revealed card offers no re-draw (#88): the draw is a commitment — re-rolling
would mean fishing for a comfortable card, which defeats the
anti-procrastination purpose. The card leaves the screen only by being
resolved: completed, snoozed/blocked ("Not now", ADR-17), or deleted. Editing
the card out of the deck (the "this card was a mistake" case) also resolves it
— the lazy restore validation clears the persisted pointer on the next check,
while the session keeps the card on screen with a resolve hint until it is
resolved on-page, so the edit never turns into a hidden re-roll (#110).
Changing the category/goal filter while a card is revealed applies to the next
draw only.

The *warm-up deal* (`POST /api/draw/warmup`, #57, ADR-30) is the "I can't
start" escape hatch on the IDLE deck: it deterministically deals the
smallest eligible card from the same pool (no weights, no probability),
rate-limited to one per `warmup_every_hours`. It is not a re-roll — the
endpoint answers 409 while a valid current draw exists, and the dealt card
is the same commitment as a drawn one, marker and badge surviving reloads
via `GET /api/draw/current`.

== 6.2 Completing a task (single transaction)

[plantuml,runtime-completion,svg]
....
@startuml
skinparam shadowing false
skinparam sequenceMessageAlign left
autonumber

actor User
participant "Client\n(card, TimerBar, Tasks page)\nor MCP client" as UI
participant "routes/tasks.ts" as Route
participant "gamificationService\ncompleteTask()" as Gam
participant "drawService" as Draw
database "SQLite" as DB

User -> UI : ✓ Done
UI -> Route : PATCH /api/tasks/:id {status: 'done'}
activate Route
Route -> DB : load task row
alt open subtasks exist
  Route --> UI : 409 "complete all subtasks first"
else
  Route -> DB : hasNonArchivedSubtask(id)?
  note right of Route
    zeroEffort (#111, ADR-32): a parent whose
    subtasks already paid the effort XP completes
    at effort 0 → the floor makes it a symbolic 1 XP.
    It is never in the deck, so wasDrawn is forced false.
  end note
  Route -> Draw : getCurrentDrawTaskId() / wasRecentlyDrawn()
  Draw --> Route : drawn := is the current draw, or\nlast_drawn_at < 6h — but NOT when that stamp\nis this task's own warm-up deal (ADR-30)

  group transaction
    Route -> Gam : completeTask(task, drawn, {effortMinutes: 0}?)
    activate Gam
    Gam -> Draw : getWarmupMarker() (read before the pointer is cleared)
    Gam -> Gam : multiplier (ADR-30):\nwarm-up? in window ×1.25 / late ×1.0 — never ×1.5\nelse min(drawn ×1.5 × momentum ×1.25, ×1.5)
    Gam -> DB : INSERT completions\n(xp = max(1, round(round(effort × impact/3) × multiplier)),\nwas_drawn, was_warmup)
    Gam -> DB : close this task's running time_entry (ADR-12)
    alt recurring
      Gam -> DB : push due_date += recur_every_days,\nclear snooze/block — task stays open
    else normal
      Gam -> DB : status = 'done', completed_at,\nclear snooze/block
    end
    Gam -> Draw : clearCurrentDraw(task.id)
    Draw -> DB : drop pointer + warm-up marker if it was this card
    Gam -> DB : freeze-token earn on a 7th/14th/… day\n(INSERT OR IGNORE — idempotent, ADR-28)
    Gam -> Gam : checkAchievements()
    Gam --> Route : {xpAwarded, bonus, newAchievements,\nrecurring, levelUp}
    deactivate Gam
    Route -> Route : maybeAutoCompleteParent(parent_id) (#111, ADR-32)
    note right of Route
      Breakdown all-done (>= 1 done, 0 open, archived
      ignored) and the parent open + non-recurring?
      The parent completes through completeTask() in the
      SAME transaction — effort 0, wasDrawn false.
    end note
  end
  Route --> UI : {task, xpAwarded, bonus, newAchievements,\nlevelUp, parentCompletion?}
end
deactivate Route
UI --> User : confetti, XP/level bar, trophy deck,\nachievement toasts — all from this one response
@enduml
....

XP, levels, streaks and the trophy deck are never stored counters — they are
derived from the `completions` log on every read (ADR-2, ADR-5), which is why
reopening a task simply deletes its latest completion row.

The client fires confetti, updates the trophy deck, and toasts new achievements
from this single response.

== 6.3 AI backward planning (with materials)

[plantuml,runtime-plan-goal,svg]
....
@startuml
skinparam shadowing false
skinparam sequenceMessageAlign left
autonumber

actor User
participant "AiSuggestionPanel\n(+ useAi)" as UI
participant "routes/ai.ts" as Route
participant "aiService" as AI
database "SQLite" as DB
participant "Claude API" as Claude

== Token gate (cheap, before anything is paid for) ==
User -> UI : pick goal + materials, write notes
UI -> Route : POST /api/ai/estimate {goalId, materialIds, userNotes?}
activate Route
Route -> Route : request-shape 400s (#84), then\n503 if no key is configured
Route -> AI : estimate({goalId, materialIds})
activate AI
AI -> AI : estimateMode() → "plan-goal"\n(an instruction would make it generate-tasks)
AI -> DB : goal, tasks, materials (files from server/data/files/)
AI -> AI : blocks = [materials (PDFs as document blocks,\ncache_control on the last), goalContext]
AI -> Claude : messages.countTokens(model, system, blocks)
Claude --> AI : {input_tokens}
AI --> Route : {inputTokens, estimatedUsd}
deactivate AI
Route --> UI : the price tag
deactivate Route
UI --> User : "≈ N tokens, ≈ $X" — confirm or deselect materials

== The paid call ==
User -> UI : confirm
UI -> Route : POST /api/ai/plan-goal {goalId, materialIds, userNotes?}
activate Route
Route -> AI : planGoal(goalId, materialIds, userNotes)
activate AI
AI -> AI : guardTokens(): re-count and enforce the limit
alt no API key
  AI --> Route : AiError 503 ai_not_configured
else > 180K input tokens
  AI --> Route : AiError 400 "deselect some materials"
else
  AI -> Claude : messages.parse(model=claude-opus-4-8,\nthinking: adaptive,\noutput_config.format = zodOutputFormat(planGoalSchema))
  Claude --> AI : parsed_output
  AI -> AI : clamp efforts to max_draw_effort
  AI --> Route : {outcomeAnalysis,\ntasks[{title, effortMinutes, impact,\nrationale, phase: now|next|later}]}
end
deactivate AI
Route --> UI : the proposal — nothing is stored yet
deactivate Route

== Review before commit ==
UI --> User : rendered proposal
User -> UI : edit / deselect / accept
UI -> Route : POST /api/tasks (one request per accepted task)
Route -> DB : INSERT tasks
Route --> UI : 201 created
@enduml
....

Nothing is written to the database without explicit user confirmation: the
estimate is a read-only price tag, the plan is a proposal, and only the tasks
the user keeps become rows.

=== Variant: generating tasks from materials (transcription mode)

....
User picks materials,
writes an instruction → POST /api/ai/estimate {goalId, materialIds, instruction}
                         └─ counts the generate-tasks prompt shape (transcription
                            system prompt + generateTasksContext), not plan-goal's
User confirms         → POST /api/ai/generate-tasks {goalId, materialIds, instruction}
                         └─ deterministic post-processing (ADR-14):
                            points→impact quintiles, split-don't-clamp
                         └─ {sourceOverview, oversizedParts, tasks[..., parts[]]}
User reviews 40+ rows  → summary header to spot-check against the material;
(edit/deselect)          part rows coupled to their exercise's checkbox
User commits           → POST /api/tasks            (one umbrella parent, no estimate)
                       → POST /api/tasks/:id/subtasks (ONE transactional batch of leaves,
                         │                            provenance in description)
                         └─ batch failed? DELETE the parent (cascade) — no
                            half-imported state (ADR-16)
....

== 6.4 Timer invariant

`POST /api/tasks/:id/timer/start` closes any running entry and opens a new one in
one transaction — at most one `time_entries` row with `ended_at IS NULL` exists.
The TimerBar restores itself after reload from `GET /api/timer/current`.

== 6.5 Focus mode (derived view)

"▶ Start now" on the revealed card starts the timer (6.4) and drops into a
fullscreen focus overlay: task title, a countdown sized to the effort
estimate, ✓ Done and ■ Stop in-face. The overlay is never persisted —
DrawPage derives it per render from the two facts above: current draw
present AND running timer on that task AND not explicitly exited (Escape)
→ focus; otherwise the revealed card or the idle deck (ADR-29). A reload
mid-focus therefore restores straight into the overlay from the same two
GETs that already restore the card (6.1) and the TimerBar (6.4); a timer
stopped or moved to another task meanwhile degrades to the plain revealed
card. The countdown is display only: past zero it flips to a visually
distinct overtime count-up — nothing is auto-stopped or auto-completed.
Escape exits the view only; the timer keeps running in the TimerBar, and
the in-face actions are the DrawPage's own (✓ Done = the completion PATCH,
■ Stop = the timer stop) — no duplicated controls, no new endpoints, and
focus sessions are ordinary `time_entries` in the Stats page's
estimates-vs-tracked block.
